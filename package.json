{
  "name": "v_posts",
  "description": "v_posts -> Vodo -> V_Todo | :D ==>> BECOMES :: v_posts.\nA simple way to save data like many solutions are doing by declaring different post types and filtering by type.\nCan be set to work similar to WordPress and Store All post data in a single JSON file.\nOr to use \"per_post_type\" mode where it will save each post type into separate file for faster access times.... taking this idea step further and we get it to separate all data into separate files that are \"per_post\" [each post entry gets separtate file...using folders to organize those files into types].",
  "version": "1.0.1",
  "author": "🢔⟬⧋⛛⧋⟭🢖",
  "bin": {
    "v_posts": "./bin/run"
  },
  "bugs": "https://github.com/V-tech-tools/v_posts/issues",
  "dependencies": {
    "@oclif/command": "^1.8.0",
    "@oclif/config": "^1.17.0",
    "@oclif/plugin-help": "^3.2.3",
    "boxen": "^5.0.1",
    "cli-progress": "^3.9.0",
    "command-line-usage": "^6.1.1",
    "enquirer": "^2.3.6",
    "faker": "^5.5.3",
    "user-home": "^3.0.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@oclif/dev-cli": "^1.26.0",
    "eslint": "^5.16.0",
    "eslint-config-oclif": "^3.1.0",
    "globby": "^10.0.2",
    "rimraf": "^3.0.2"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "files": [
    "/bin",
    "/npm-shrinkwrap.json",
    "/oclif.manifest.json",
    "/src"
  ],
  "homepage": "https://github.com/V-tech-tools/v_posts",
  "keywords": [
    "oclif"
  ],
  "license": "MIT",
  "main": "src/index.js",
  "oclif": {
    "commands": "./src/commands",
    "bin": "v_posts",
    "plugins": []
  },
  "repository": "V-tech-tools/v_posts",
  "scripts": {
    "postpack": "rimraf oclif.manifest.json",
    "posttest": "eslint .",
    "prepack": "oclif-dev manifest && oclif-dev readme",
    "test": "echo NO TESTS",
    "version": "oclif-dev readme && git add README.md"
  }
}
